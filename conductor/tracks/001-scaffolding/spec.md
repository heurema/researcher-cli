# Specification: Initial Scaffolding

Set up the Rust project structure adhering to the CLI Doctrine. Define the base JSON envelope for requests and responses. Implement the `src/contract`, `src/core`, and `src/adapters` directories.