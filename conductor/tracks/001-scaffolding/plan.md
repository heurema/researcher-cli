# Plan: Initial Scaffolding

1. `cargo init`
2. Update `Cargo.toml` with `serde`, `serde_json`, `clap`.
3. Create `src/contract/schema.rs` defining the base response envelope.
4. Create `src/core/mod.rs` and `src/adapters/mod.rs`.